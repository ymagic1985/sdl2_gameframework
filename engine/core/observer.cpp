#include "observer.h"


Observer::~Observer() {
}

//place holders 
//void Achivements::OnNotify(const Entity& entity, Event event) {
    /*switch (event) {
         case: EVENT_START_FALL:
             if(entity.isHero() && heroIsOnbridge_)
                 unlock(Achievment::ACHIEVEMENT_FELL_OF_BRIDGE);
                 break;
         case: EVENT_LEVEL_UP:
         //Do something
                 break;
         default:
                 break;
    }*/
//}

void Achievements::unlock(Achievement achievement) {
}
