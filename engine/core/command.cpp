#include "command.h"
#include "gameObject.h"

void InputHanlder::handleInput() {
    /*
    if(isPresssed(BUTTON_X) buttonX_->execute();
    if(isPresssed(BUTTON_Y) buttonY_->execute();
    if(isPresssed(BUTTON_A) buttonA_->execute();
    if(isPresssed(BUTTON_B) buttonB_->execute();
    */
}

void JumpCommand::execute(GameObject& go) {
   // go.Jump();
}

void FireCommand::execute(GameObject& go) {
   // go.Fire();
}
